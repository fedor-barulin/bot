class KnowledgeRouter:
    """Маршрутизация запроса по разделам базы знаний на основе анализа.
    
    Возвращает пустой список — фильтрация по заголовку документа не применяется,
    т.к. семантический векторный поиск + реранкер сами находят релевантные чанки.
    При необходимости можно вернуть список title-значений для фильтрации.
    """

    def route(self, query_analysis: dict) -> list[str]:
        return []
